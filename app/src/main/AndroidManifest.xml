<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешение для ForegroundService -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab09"
        tools:targetApi="31">

        <!-- Стартовый экран: SplashActivity -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Экран логина -->
        <activity android:name=".activities.LoginActivity" android:exported="true" />

        <!-- Домашний экран -->
        <activity android:name=".activities.HomeActivity" android:exported="true" />

        <!-- Экран профиля (добавлено!) -->
        <activity android:name=".activities.ProfileActivity" android:exported="true" />

        <!-- MainActivity оставляем (без интент-фильтра) -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- Музыкальный сервис -->
        <service android:name=".service.MusicService" />

        <activity android:name=".activities.MyMusicActivity" android:exported="true" />

    </application>

</manifest>
